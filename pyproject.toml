[project]
name = "quant-project"
version = "0.0.1"
dependencies = [
    "pandas",
    "numpy",
    "pyarrow",
    "SQLAlchemy",
    "pydantic",
    "pydantic-settings",
    "yfinance",
    "streamlit",
]


[project.optional-dependencies]
dev = ["pytest", "watchdog"]


[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
